import { Module } from '@nestjs/common';

/**
 * Placeholder para operações de tenant (listar membros, convidar, etc).
 * Na Fase 1 o tenant é criado automaticamente via webhook Clerk.
 */
@Module({})
export class TenantsModule {}
